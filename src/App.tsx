import { useState } from "react";

import "./App.css";

function App(): React.JSX.Element {
  const [count, setCount] = useState(0);

  return (
    <>
      <div>APP</div>
    </>
  );
}

export default App;
